// Exclude routes from manifests used for dynamic SSR
export const prerender = true

// Disable server-side rendering to turn this into SPA
// Fallback is defined in svelte.config.js
export const ssr = false
